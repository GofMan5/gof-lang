fn add(a: int, b: int) -> int:
    return a + b

fn sub(a: int, b: int) -> int:
    return a - b

fn calculate() -> int:
    base: int = add(120, 30)
    mut total: int = sub(base, 50)
    total = add(total, 7)
    return total

fn main() -> int:
    return calculate()
