fn main() -> Result[int, RuntimeError]:
    path = "target/io_roundtrip.txt"
    write_file(path, "gof-io")?
    text = read_file(path)?
    assert(contains(text, "io"), "missing io")
    return Result.Ok(len(text))
