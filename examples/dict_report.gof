fn total(metrics: dict) -> int:
    return metrics["ok"] + metrics["warn"] + len(metrics)

fn main() -> int:
    metrics: dict = {"ok": 7, "warn": 2}
    assert(contains(metrics, "ok"), "missing ok")
    return total(metrics)
