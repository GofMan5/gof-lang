fn factorial(n: int) -> int:
    mut current: int = n
    mut total: int = 1

    while current > 1:
        total = total * current
        current = current - 1

    if total == 120:
        return total
    else:
        return 0

fn main() -> int:
    return factorial(5)
